# Python
__pycache__/
*.pyc
.venv/

# secrets
.env

# model weights (too large for git; download instructions in README)
models/*_best/
models/*_checkpoints/

# old frontend scaffold copy (superseded by the HititFinLex repo; has its own nested git repo)
frontend/

# published as HititFinLex/dataset/ at repo root instead
HititFinLex_VeriSeti_2.zip

# misc caches
*.tsbuildinfo

# evaluation local outputs (generated per machine)
evaluation/*.local.json
evaluation/*.latest.json

# local runtime output
runtime_logs/
uvicorn*.log
